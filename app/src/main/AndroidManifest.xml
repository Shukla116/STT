<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.speechtotext">

    <!-- Mic permission - zaruri hai -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Internet optional - online fallback ke liye -->
    <uses-permission android:name="android.permission.INTERNET" />
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_mic"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_mic"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpeechToText">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Google Speech Recognizer Service query - Android 11+ ke liye zaruri -->


    </application>
</manifest>
